
# Launch a bash session inside a running container:
# $ _bash api_web_1
#
function _bash() {
    docker exec -it $1 bash
}

# From any bash session, points Docker to the default VM
# $ _vm
#
function _vm() {
    eval "$(docker-machine env default)"
    env|sort|grep DOCKER
}

function _shell() {
    docker exec -it api_web_1 /venv/api/bin/python manage shell
}

function _recreate() {
    docker exec -it api_web_1 /venv/api/bin/python manage data recreate
}

function _populate() {
    docker exec -it api_web_1 /venv/api/bin/python manage data populate
}
