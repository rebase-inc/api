syncs:
  code2resume-sync: 
    sync_host_ip: 'dev'
    sync_strategy: 'rsync'
    src: '../code2resume'
    dest: '/code'
    sync_host_port: 10873
    sync_excludes: ['node_modules/', '.gitignore', '.git/']

