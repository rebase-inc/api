common_env:
  environment:
    APP_SETTINGS: "/api/rebase/common/pro.py"
    GITHUB_CLIENT_SECRET:
    NOTIFICATION_EMAIL_PASSWORD:
    PYTHONHASHSEED:
    REBASE_CLIENT_HOST:
    REBASE_CLIENT_PORT:
    SECRET_KEY:

webserver:
  image: rebase/api
  volumes:
   - ..:/api
  extends:
    service: common_env
